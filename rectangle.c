#include <stdio.h>
main()
{
    int a, b;

    printf("Enter the value of Base and Height:\n");
    scanf("%d %d", &a, &b);

    printf("Area of rectangle is : %d", a * b);

    return 0;
}
